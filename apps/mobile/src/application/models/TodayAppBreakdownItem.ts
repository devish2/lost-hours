import type { AppUsageBreakdownItem } from './AppUsageBreakdownItem';

/** Read-only per-app Today analytics row (grouped by Android packageName). */
export type TodayAppBreakdownItem = AppUsageBreakdownItem;
